import { SxProps, Theme } from "@mui/material/styles";

export const uploadGridStyles: SxProps<Theme> = {
  justifyContent: 'center',
  alignItems: 'center',
}
export const buttonStyles: SxProps<Theme> = {
  position: 'relative',
  width: '100%',
}
export const errorTypographyStyles: SxProps<Theme> = {
  textAlign: 'center',
  color: 'red',
}