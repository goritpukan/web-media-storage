export interface IVideoPreview {
  id: string;
  name: string;
  previewUrl: string;
}
export interface IVideo {
  id: string;
  name: string;
  description: string;
  authorId: string;
  videoUrl: string;
}
